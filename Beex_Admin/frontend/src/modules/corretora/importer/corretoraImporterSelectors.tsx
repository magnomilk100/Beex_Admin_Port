import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const corretoraImporterSelectors = importerSelectors(
  'corretora.importer',
);

export default corretoraImporterSelectors;
