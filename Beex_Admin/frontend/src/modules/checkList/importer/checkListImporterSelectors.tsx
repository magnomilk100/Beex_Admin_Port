import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const checkListImporterSelectors = importerSelectors(
  'checkList.importer',
);

export default checkListImporterSelectors;
